# Copy to backend/.env or set when running the backend.
# npm start and npm run contract:deploy write CONTRACT_ADDRESS automatically.

# Deployed Voting contract address (set by deploy script)
CONTRACT_ADDRESS=

# Ganache or other Ethereum RPC
RPC_URL=http://127.0.0.1:8545

# Backend (and frontend) server port
PORT=3000
